{
  "name": "readsnap",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension: screenshot pages, OCR with Tesseract.js, send to Readwise Reader",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && node scripts/build.js"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "tesseract.js": "^5.1.1"
  }
}
